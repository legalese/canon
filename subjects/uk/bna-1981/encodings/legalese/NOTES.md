# uk/bna-1981 — `legalese` row

Free prose for humans. **No script reads this file.**

## 0. Where this row came from

Deposited on 2026-09-23 from `legalese/l4-ide`, byte-identical to `jl4/examples/legal/bna/` at l4-ide `7df7a3ca6`.
Every file of that directory is here:

| was (l4-ide `jl4/examples/legal/bna/`) | is |
| --- | --- |
| `bna.l4`, `tests/*` | `bna.l4`, `tests/*` |
| `README.md` | `README.md` |
| `SMOKE-REPORT.md` | `report/SMOKE-REPORT.md` |
| `bna.dmn`, `bna.dmn.md`, `bna.dmn.fidelity.txt`, `bna.fidelity.txt`, `bna.cases.json` | `projections/` |
| `source-s1.txt` | `../../source/source-s1.txt` |

The history is in l4-ide.
`README.md` was written for the old layout, so its one relative link, to `source-s1.txt`, and its section "Why this lives in jl4/examples/legal/" describe where the files used to be.
They are left as deposited, and this table is the correction.
l4-ide's vendored mirror places this row at `jl4/examples/canon/uk/bna-1981/`.

## 1. What it is

This is the demo pipeline's smoke-test subject.
It is section 1 of the Act, encoded by hand, stage by stage, so that what was done became the requirements for l4-ide's `go` orchestrator.
Read `README.md` for the scope, the twelve-row ambiguity register and the tests, and `report/SMOKE-REPORT.md` for the run.
The pre-existing `british-citizen-act.l4` in l4-ide's `jl4/examples/legal/` is an unrelated toy and shares nothing with this row.

## 2. Terms

OGL v3.0.
The attribution is a licence condition, and it is recorded in `NOTICE`.
See `SOURCE-LICENSE.md` for what was read, and when.

## 3. Not reviewed

No HG1 grant or waiver was found for this encoding.
