האגרה לפי `vintage` בעד `service` with `the Regulations as amended` and `theoretical examination`
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the SimpLEX draft` and `appeal against a practical examination score`
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the SimpLEX draft` and `appeal against a practical examination score` is equal to `JUST` with 313
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the SimpLEX draft` and `appeal against a theoretical examination score` is equal to `JUST` with 313
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the Regulations as made` and `appeal against a practical examination score` is equal to `JUST` with 234
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the Regulations as made` and `appeal against a theoretical examination score` is equal to `NOTHING`
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the Regulations as amended` and `appeal against a theoretical examination score` is equal to `JUST` with 198
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the Regulations as amended` and `appeal against a practical examination score` is equal to `NOTHING`
הסכום הכולל שיש לשלם לפי `vintage` בעד `service` with `the Regulations as amended` and `receipt or renewal of a licence` is equal to `JUST` with 284
האגרה בסך `fee` שקלים חדשים מעודכנת לפי שיעור השינוי של המדד החדש `new index` לעומת המדד היסודי `base index`, ומעוגלת לשקל החדש השלם הקרוב with 194, 105.3 and 104.1 is equal to 196
האגרה בסך `fee` שקלים חדשים מעודכנת לפי שיעור השינוי של המדד החדש `new index` לעומת המדד היסודי `base index`, ומעוגלת לשקל החדש השלם הקרוב with 284, 105.3 and 104.1 is equal to 287
האגרה בסך `fee` שקלים חדשים מעודכנת לפי שיעור השינוי של המדד החדש `new index` לעומת המדד היסודי `base index`, ומעוגלת לשקל החדש השלם הקרוב with 398, 105.3 and 104.1 is equal to 403
תחילתן של התקנות שבעה ימים מיום פרסומן ביום `published` with `YMD` with 2025, 7 and 9 is equal to `YMD` with 2025, 7 and 16
